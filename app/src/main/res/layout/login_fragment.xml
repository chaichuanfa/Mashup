<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
                name="viewmodel"
                type="com.felix.mashup.controller.login.LoginViewModel"
                />

        <variable
                name="fragment"
                type="com.felix.mashup.controller.login.ui.LoginFragment"
                />
    </data>

    <RelativeLayout
            xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clickable="true"
            android:focusable="true"
            android:fitsSystemWindows="true"
            android:background="@color/black"
            tools:context="com.felix.mashup.controller.login.ui.LoginFragment"
            >


        <Button
                android:id="@+id/finish"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:onClick="@{fragment::finishActivity}"
                />

    </RelativeLayout>
</layout>
